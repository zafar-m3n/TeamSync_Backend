// Placeholder — fully replaced in Phase 2 (Models).
// Phase 2 will export all Sequelize models plus their associations
// from this file as a single `db` object, e.g.:
//   const db = require('../models');
//   db.Employee, db.User, db.Permission, etc.

module.exports = {};
